package weather

import (
	"testing"
)

func TestGetForecast(t *testing.T) {
	// // TODO: use some proper mocked API response
	// location := location.Location{
	// 	Latitude:  48.864716,
	// 	Longitude: 2.349014,
	// }
	// timerange := timerange.TimeRange{
	// 	Start: time.Date(2024, 3, 31, 0, 0, 0, 0, time.FixedZone("GMT", 0)),
	// 	End:   time.Date(2024, 3, 31, 23, 59, 59, 59, time.FixedZone("GMT", 0)),
	// }
	// forecast, err := GetForecast(location, timerange)
	// assert.Nil(t, err)
	// assert.Equal(t, 24, len(forecast.CloudCover))
	// assert.Equal(t, 24, len(forecast.Temperatures))
	// assert.Equal(t, 24, len(forecast.Precipitation))
}
